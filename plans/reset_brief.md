- Failed Artifact: `plans/plan_v1.md` (Revision v1)
- Reset Subject: fresh writing plan for recall-system MVP v1 SKILL.md harness
- Fail Anchor: Seed requires learning materials at `/home/user01/project/recall-system/subject/<topic_slug>/index.md` but the failed plan uses `subjects/` directory name; review_queue.json fields not explicitly listed (only referenced as "all required fields"); 12 boundary conditions only enumerated by number without explicit handling text in all sub-skills; Oracle Browser default search and optional user-provided material input missing from learn mode contract
- Required Reset: Replace `subjects/` with exact Seed path `subject/<topic_slug>/index.md`; explicitly list all review queue fields (concept, question, failure_reason, last_score, next_priority, last_seen, due_hint) in the plan; handle each of the 12 BCs with explicit content text (not just enumeration or references); add Oracle Browser default web search option and optional user material input to learn sub-skill contract
- Do Not Carry Forward: `subjects/` directory name (must use Seed's `subject/<topic_slug>/index.md`); bare reference to "all required fields" without listing them; BC enumeration without explicit handling text for each condition
